embedding(尤其是word embedding，或者带representation的，表示学习)
knowledge（尤其是knowledge graph。knoledge base，也行吧）
completion（这个应该是补全）
semantic
representation
disambiguation/ting（消岐）
natural language
acquisition（获取，尤其是konwledge acquisition）
reason(推理，但是还没碰到过)
alignment
named entity recognition（命名实体识别）

KBC(knowledge base completion) link prediction,论文里看的，解决知识库不完整（incomplete）的问题的，extend knowlwdge base 


latent feature model(一个补全的模型)



embedding现在是很流行的